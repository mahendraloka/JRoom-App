<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Izin internet untuk akses API -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RoomBook"
        tools:targetApi="31"
        android:forceDarkAllowed="false">


        <!-- ✅ LoginActivity jadi launcher -->
        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ Tambahkan RegisterActivity -->
        <activity android:name=".RegisterActivity" />

        <!-- Activity Utama -->
        <activity android:name=".MainActivity" />

        <!-- Cek Ketersediaan -->
        <activity android:name=".CheckAvailabilityActivity" />
        <activity android:name=".BookingActivity" />
        <activity android:name=".RoomStatusActivity" />
        <activity android:name=".WebViewActivity" />
        <activity android:name=".RiwayatBookingActivity" />
    </application>

</manifest>
